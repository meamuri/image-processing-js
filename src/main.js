let toByte = (val) => {
  if (val > 255) {
      return 255;
  }
  if (val < 0)
      return 0;
  return val;
};

let processingBrightness = (ctx, brightness) => {

};

let main = () => {

};


$(document).ready(main);
